// import { createClient } from "@/libs/supabase/server";

// export const getProfile = async (authId: string) => {
//   const supabase = createClient();
//   return await supabase.from("").select("*").eq("id", authId).single();
// };
